<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.vtusgpacalculator">

    <application
        android:allowBackup="true"
        android:icon="@drawable/sgpa"
        android:label="@string/app_name"
        android:roundIcon="@drawable/sgpa"
        android:supportsRtl="true"
        android:theme="@style/Theme.VTUSGPACalculator">
        <activity android:name=".SGPAPage" android:parentActivityName=".MainActivity"></activity>
        <activity android:name=".CGPAPage" android:parentActivityName=".MainActivity"></activity>
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>